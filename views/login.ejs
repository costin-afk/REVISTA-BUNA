<!DOCTYPE html>
<html lang="ro">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Admin Login – Football Magazine</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;600&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    background: #0a0a0a;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Inter', sans-serif;
    background-image: radial-gradient(ellipse at 50% 0%, #1a3a1a 0%, #0a0a0a 60%);
  }
  .login-box {
    background: #111;
    border: 1px solid #222;
    border-radius: 16px;
    padding: 48px 40px;
    width: 100%;
    max-width: 400px;
    text-align: center;
  }
  .logo {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 48px;
    color: #00e676;
    letter-spacing: 4px;
    line-height: 1;
    margin-bottom: 6px;
  }
  .subtitle {
    color: #555;
    font-size: 13px;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-bottom: 40px;
  }
  label {
    display: block;
    text-align: left;
    color: #888;
    font-size: 12px;
    letter-spacing: 1px;
    text-transform: uppercase;
    margin-bottom: 6px;
  }
  input[type=text], input[type=password] {
    width: 100%;
    background: #1a1a1a;
    border: 1px solid #2a2a2a;
    border-radius: 8px;
    color: #fff;
    padding: 12px 16px;
    font-size: 16px;
    margin-bottom: 20px;
    outline: none;
    transition: border-color 0.2s;
  }
  input:focus { border-color: #00e676; }
  .btn {
    width: 100%;
    background: #00e676;
    color: #000;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 22px;
    letter-spacing: 3px;
    border: none;
    border-radius: 8px;
    padding: 14px;
    cursor: pointer;
    transition: background 0.2s;
  }
  .btn:hover { background: #00c853; }
  .error {
    background: #2a1a1a;
    border: 1px solid #c62828;
    color: #ef9a9a;
    border-radius: 8px;
    padding: 10px 14px;
    font-size: 14px;
    margin-bottom: 20px;
  }
</style>
</head>
<body>
<div class="login-box">
  <div class="logo">⚽ MATCH<br>MAG</div>
  <div class="subtitle">Admin Panel</div>
  <% if (error) { %>
    <div class="error"><%- error %></div>
  <% } %>
  <form method="POST" action="/admin/login">
    <label>Utilizator</label>
    <input type="text" name="username" required autocomplete="username">
    <label>Parolă</label>
    <input type="password" name="password" required autocomplete="current-password">
    <button type="submit" class="btn">INTRĂ</button>
  </form>
</div>
</body>
</html>
